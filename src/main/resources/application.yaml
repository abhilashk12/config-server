server:
  application: config-server
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abhilashk12/ecommerce-config-repo.git

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL}